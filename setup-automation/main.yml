---
# =============================================================================
# setup-automation/main.yml
# Runs inside the zerotouch chart's setup-automation init container at pod start.
# Use this for pre-provisioning lab resources (configuring services, etc.)
#
# NOTE: The ZT runner is deployed natively by the zerotouch chart.
# No deployment patching needed here.
# =============================================================================

- name: ZT Lab setup
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Setup placeholder — add lab-specific tasks here
      ansible.builtin.debug:
        msg: "Setup complete — no pre-provisioning needed for this lab"
