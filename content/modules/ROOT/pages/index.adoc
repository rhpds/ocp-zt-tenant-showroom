= Welcome

This lab tests two automation approaches across two modules:

* *Ansible-automatable* — tasks done via `oc` CLI (kubernetes.core can validate these)
* *UI-required* — tasks done in the OCP web console (Demolition Playwright can simulate these)

Your namespace: `{student_ns}`
Your username: `{student_user}`
