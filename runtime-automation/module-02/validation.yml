---
- name: Module 2 Validation — check Secret in ztworkspace
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Read showroom-userdata ConfigMap
      ansible.builtin.uri:
        url: >-
          https://kubernetes.default.svc/api/v1/namespaces/{{
            lookup('file', '/var/run/secrets/kubernetes.io/serviceaccount/namespace')
          }}/configmaps/showroom-userdata
        headers:
          Authorization: "Bearer {{ lookup('file', '/var/run/secrets/kubernetes.io/serviceaccount/token') }}"
        validate_certs: false
        status_code: 200
      register: r_userdata

    - name: Parse user data
      ansible.builtin.set_fact:
        student_ns2: "{{ (r_userdata.json.data['user_data.yml'] | from_yaml).user }}-ztworkspace"

    - name: Check Secret exists
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Secret
        name: lab-status
        namespace: "{{ student_ns2 }}"
      register: r_secret

    - name: Validate Secret
      validation_check:
        check: "{{ r_secret.resources | length > 0 }}"
        pass_msg: "Secret lab-status exists in {{ student_ns2 }}"
        error_msg: |
          Secret lab-status not found in {{ student_ns2 }}.

          Create it:
            oc create secret generic lab-status \
              --from-literal=status=complete \
              -n {{ student_ns2 }}
